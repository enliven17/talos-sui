[package]
name = "talos_name_service"
version = "0.1.0"
edition = "2024.beta"
license = "MIT"
authors = ["Talos Protocol"]

# No explicit Sui dependency — the Sui CLI auto-injects the framework
# at the version baked into the installed CLI binary. See
# talos_registry/Move.toml for the same pattern.
[dependencies]

[addresses]
talos_name_service = "0x0"
